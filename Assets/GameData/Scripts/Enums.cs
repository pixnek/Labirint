namespace Labirint
{
    public enum GameUIButtonType
    {
        Restart
    }
    public enum FollowerState
    {
        Empty,
        HasTarget,
        Following
    }
    public enum FollowerType
    {
        Ally,
        Enemy
    }
}